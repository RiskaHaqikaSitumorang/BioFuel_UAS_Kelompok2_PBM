<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/dialog_bg"
    android:layout_margin="24dp"
    android:gravity="center"
    android:padding="32dp">

    <!-- Success Icon with Animation -->
    <FrameLayout
        android:layout_width="160dp"
        android:layout_height="160dp"
        android:layout_marginBottom="24dp">

        <!-- Background Circle -->
        <View
            android:layout_width="160dp"
            android:layout_height="160dp"
            android:background="@drawable/circle_bg_light_orange"
            android:layout_gravity="center" />

        <!-- Coin Stack Icon -->
        <ImageView
            android:id="@+id/iv_coins"
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_coin_stack"
            android:scaleX="0"
            android:scaleY="0" />

    </FrameLayout>

    <!-- Success Title -->
    <TextView
        android:id="@+id/tv_selamat"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Selamat"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="#333333"
        android:layout_marginBottom="8dp" />

    <!-- Success Message -->
    <TextView
        android:id="@+id/tv_message"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Penukaran poin berhasil"
        android:textSize="16sp"
        android:textColor="#666666"
        android:layout_marginBottom="4dp"
        android:gravity="center" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Silahkan periksa saldo sebelum kembali"
        android:textSize="14sp"
        android:textColor="#999999"
        android:layout_marginBottom="32dp"
        android:gravity="center" />

    <!-- Back Button -->
    <Button
        android:id="@+id/btn_kembali"
        android:layout_width="200dp"
        android:layout_height="48dp"
        android:background="@drawable/button_brown_rounded"
        android:backgroundTint="@null"
        android:elevation="2dp"
        android:text="Kembali"
        android:textColor="#FFFFFF"
        android:textSize="16sp"
        android:textStyle="bold"/>
</LinearLayout>